import { NotFoundPage } from '@/components/pages/simple-pages'

/** The locale is not available here, so the shared English wording is used. */
export default function NotFound() {
  return <NotFoundPage locale="en" />
}
